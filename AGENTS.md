# Repository Workflow Rules

- After any meaningful, user-visible milestone or rollback point is reached, create a git commit proactively instead of waiting for the user to ask. This repository should keep recoverable checkpoints so later reversions can be done from git history rather than by re-describing code changes in chat.
